# M4 — README Full Refresh
depends: M3

## Phase 1: Update README.md
- Update tech stack table: remove tRPC, add "Hono REST API" as the API layer, remove superjson
- Update project structure diagram: remove `server/trpc/` directory, reflect actual post-M3 file tree
- Update "Quick Start" and "Local Development" sections if any steps reference tRPC
- Update "Scripts Reference" table if any scripts change
- Replace any inline tRPC references (e.g. "tRPC router & procedures" in the structure comment) with REST equivalents
- Review all content for staleness — update anything that no longer matches the codebase
