# M1 — Landing Page with Hero Section

## Phase 1: Create landing page

- Create a new route at `/` in the React client that shows a landing page
- Add a hero section with:
  - App name heading: "My Hono App"
  - Subtitle: "A fullstack Cloudflare Workers app"
  - A "Get Started" button that links to `/dashboard`
- Style using Tailwind CSS
- Keep it simple — single component in `client/pages/Landing.tsx`
- Add the route to the React Router config in `client/App.tsx`
