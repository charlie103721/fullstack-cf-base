# M3 — Remove tRPC, Replace with Hono REST Routes
status: done
startedAt: 2026-03-05T14:55:00Z
completedAt: 2026-03-05T15:22:00Z
duration: 27m0s

## Phase 1: Server — Convert tRPC to Hono REST Routes
- Convert `server/features/hello/router.ts` from tRPC procedure → Hono route handler (`GET /api/hello?name=X`), return JSON `{ message, timestamp }`
- Convert `server/features/users/router.ts` from tRPC procedures → Hono route handlers (`GET /api/users`, `POST /api/users`), keep repo/service/schema layered pattern
- Replace `TRPCError` in `server/features/users/service.ts` with Hono `HTTPException`
- Add auth guard helper for protected routes — check `c.get("user")`, return 401 if null
- Update `server/index.tsx`: remove `@hono/trpc-server` import and `trpcServer` middleware (lines 4-5, 41-50), register new Hono feature routes via `app.route()`
- Write replacement test for hello route using Hono `app.request()` (replaces `server/trpc/router.test.ts`)

## Phase 2: Client — Switch from tRPC Hooks to React Query + Fetch
- Rewrite `client/src/lib/api.ts`: remove tRPC client, export a `fetchApi` wrapper and keep `queryClient` export
- Update `client/src/pages/Home.tsx`: replace `trpc.hello.useQuery()` → `useQuery` with fetch to `GET /api/hello`
- Update `client/src/pages/Users.tsx`: replace `trpc.users.list.useQuery()` and `trpc.users.create.useMutation()` → `useQuery`/`useMutation` with fetch to `GET /api/users` and `POST /api/users`
- Update `client/src/main.tsx`: remove `trpc.Provider` wrapper, keep `QueryClientProvider`

## Phase 3: Cleanup — Delete tRPC Files and Packages
- Delete entire `server/trpc/` directory (`trpc.ts`, `router.ts`, `index.ts`, `router.test.ts`)
- Remove packages from `package.json`: `@hono/trpc-server`, `@trpc/client`, `@trpc/react-query`, `@trpc/server`, `superjson`
- Remove `@trpc/client`, `@trpc/react-query`, `superjson` from `optimizeDeps.include` in `vite.config.ts`
- Run `bun install` to update lockfile
- Verify build + tests pass
